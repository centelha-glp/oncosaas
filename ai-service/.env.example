# AI Service (FastAPI)
BACKEND_URL=https://localhost:3002
BACKEND_SERVICE_TOKEN=change-me-internal-service-token
# Local backend HTTPS usa certificado self-signed. Em staging/prod, mantenha true.
BACKEND_TLS_VERIFY=false
# ENVIRONMENT=staging ou production exige BACKEND_SERVICE_TOKEN (503 se vazio), mesmo sem flag abaixo.
# ENVIRONMENT=development (padrão local) permite endpoints sem token se BACKEND_SERVICE_TOKEN estiver vazio.
# Opcional [C-01]: true/1/yes/on exige token em qualquer ambiente (503 se BACKEND_SERVICE_TOKEN vazio).
AI_SERVICE_REQUIRE_SERVICE_TOKEN=
# SEC-002: chamadas do Nest à observabilidade enviam X-Tenant-Auth (HMAC-SHA256 do tenant com este token).

OPENAI_API_KEY=
ANTHROPIC_API_KEY=

CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3002,http://127.0.0.1:3002

# Transcrição de áudio na extração de exames (Whisper via OpenAI)
# EXAM_AUDIO_TRANSCRIBE_MODEL=whisper-1  (modelo da API audio/transcriptions)
# EXAM_WHISPER_PROMPT_EXTRA=  termos pt-BR adicionais separados por espaço; vão ao fim do prompt (maior prioridade no truncamento)

# Optional tuning
RAG_EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
RAG_TOP_K=4
RAG_SCORE_THRESHOLD=0.30

# Modelos de chat LLM (opcional; vazios = inferir provedor pelas chaves e modelos padrão por provedor)
# LLM_DEFAULT_PROVIDER=anthropic|openai  (vazio = preferir anthropic se ANTHROPIC_API_KEY existir)
# LLM_DEFAULT_MODEL=  (vazio = claude-haiku-4-5 ou gpt-4o-mini conforme o provedor)
# LLM_FALLBACK_PROVIDER=
# LLM_FALLBACK_MODEL=
LLM_ORCHESTRATOR_MODEL=claude-haiku-4-5
LLM_SUBAGENT_MODEL=claude-haiku-4-5
LLM_OPENAI_AGENTIC_MODEL=gpt-4o
