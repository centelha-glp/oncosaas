# Backend service env (can be sourced from root .env)
NODE_ENV=development
PORT=3002

DATABASE_URL=postgresql://ONCONAV:ONCONAV_dev@localhost:5432/ONCONAV_development
REDIS_URL=redis://localhost:6379
RABBITMQ_URL=amqp://ONCONAV:ONCONAV_dev@localhost:5672

JWT_SECRET=change-me-jwt-secret
JWT_EXPIRES_IN=24h
ENCRYPTION_KEY=change-me-32-byte-encryption-key

FRONTEND_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://localhost:3000,https://127.0.0.1:3000

AI_SERVICE_URL=http://localhost:8001
BACKEND_SERVICE_TOKEN=change-me-internal-service-token

# Local/staging convenience only.
RUN_SEED_ON_BOOT=true

# Production overrides:
# NODE_ENV=production
# RUN_SEED_ON_BOOT=false
# DATABASE_URL=<managed-postgres-url>
# ALLOWED_ORIGINS=https://app.yourdomain.com
